---
import BaseLayout from '../layouts/BaseLayout.astro';
import { blog } from '../config';
---
<BaseLayout title="写作" description="用 GitHub 登录写作后台，编辑 Markdown 并发布文章。" noindex>
  <section class="writing">
    <h1>写作</h1>
    <p>用 GitHub 登录 Pages CMS，编辑 Markdown，保存后自动发布。</p>
    <a class="write-link" href={blog.editor} rel="noreferrer">进入写作后台 ↗</a>
    <details>
      <summary>首次使用</summary>
      <ol>
        <li>选择 <strong>Sign in with GitHub</strong>，使用作者账号 <strong>oqwn</strong> 登录。</li>
        <li>安装 Pages CMS 的 GitHub App，选择 <strong>Only select repositories → wzj-blog</strong>。</li>
        <li>打开 <strong>main → 文章</strong>。正文可切换 <strong>Source</strong> 和 <strong>Editor</strong>；关闭「草稿」后保存即可发布。</li>
      </ol>
      <p>无需手动输入令牌。登录过期时重新登录即可；访客打开此入口不会获得写入权限。</p>
    </details>
    <p class="writing-note">草稿不显示在博客中，但源文件仍在公开仓库中可见。</p>
    <a class="deploy-link" href={`${blog.repository}/actions/workflows/pages.yml`}>查看发布进度</a>
  </section>
</BaseLayout>

<style>
  .writing { padding-top: 26px; }
  h1 { font-size: 20px; font-weight: 600; margin-bottom: 10px; }
  p { color: var(--muted); }
  .write-link { display: inline-block; margin: 20px 0 24px; padding: 8px 15px; border-radius: 5px; background: var(--ink); color: var(--paper); font-weight: 500; }
  .write-link:hover { background: var(--blue); text-decoration: none; }
  details { border-top: 1px solid var(--line); padding-top: 16px; font-size: 13px; }
  summary { cursor: pointer; width: fit-content; }
  ol { padding-left: 22px; margin: 12px 0; }
  li { margin: 8px 0; }
  .writing-note { margin-top: 20px; font-size: 12px; }
  .deploy-link { display: inline-block; margin-top: 8px; font-size: 12px; color: var(--blue); }
</style>
